import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  boxPaper: {
    padding: 10,
    textAlign: "center",
    overflow: "auto",
    maxHeight: "380px",
  },
});

export default useStyles;
