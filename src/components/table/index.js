const Table = ({getCoordinates}) => {
    const board = createBoard

    const createBoard = () => {
        let arr = []
        for(let i = 0; i < 8; i++){
            for(let j = 0; j < 8; j++){
                arr[i].push
            }
        }
        return arr
    }
    
    return(
        <h1>Table</h1>
        
    )
}

export default Table